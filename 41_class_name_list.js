// it will change the class-css from red to text yellow
// one.className="text yellow"

// it will give the shole class list of that given id
console.log(one.classList);

// it will remove a class from that id
one.classList.remove("red");

// it will add a class to that id
one.classList.add("red");

// it will add the class if it exists, otherwise removes it
one.classList.toggle("yellow")
console.log(console);    // it will give all the functions of console
console.log("Hello I am Nafeesa");   // it will print the output
console.error("This is an error");   // it will be in the form of error
console.assert(5>33);

console.assert();
console.assert(555>33);
console.clear();    // it will clear the console

let obj={
    a : 1,
    b : 2,
    c : 3,
}
console.table(obj);  // it will give the output in the form of a table

console.warn("Please do not smoke");

// it just like .log, but when we filter it will appear in info 
console.info("This is an important info");

let a;
// this will help us to tell how much time we have taken
console.time(a);
console.timeEnd(a);

// we can also compare the time of two loops and can know that how much time they have taken
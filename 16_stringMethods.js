// length method
let name1 = "Nafeesa Mushtaq";
console.log(name1.length);

// used to convert into upper case
console.log(name1.toUpperCase());

// used to convert in lower case
console.log(name1.toLowerCase());

// it will print the strings from starting index to last index
console.log(name1.slice(2,4));
console.log(name1.slice(2));

// it will reolace the word with the given word
console.log(name1.replace("Nafeesa","Aleezy"));

// it will concatinate the word with the string
console.log(name1.concat("Noor"));

// t will remove the extra sapces
console.log(name1.trim());
